# 🎤 KPOP之王巔峰賽 — 使用說明

一套完全離線、零安裝的互動測驗 Web App。技術棧：**React 18 + Tailwind CSS**（已離線內嵌，不需要 Node.js、不需要 npm install、不需要架伺服器）。

---

## 1. 快速開始

| 我想做什麼 | 雙擊這個檔案 |
|---|---|
| 開始玩 | `啟動遊戲.bat` |
| 新增素材後重建題庫 | `更新題庫.bat` |
| 把原版 MV 自動做成第三關黑影題目 | `產生第三關剪影.bat` |
| 第一次設定 GitHub 版控 | `Git設定.bat` |
| 改完東西存一版到 GitHub | `Git存檔.bat` |
| 打包成網頁版（傳連結給朋友玩） | `建立網頁版.bat` |

`啟動遊戲.bat` 會先自動掃描一次題庫，再用預設瀏覽器打開遊戲。
（也可以直接雙擊 `app\index.html`，只是不會順便更新題庫。）

> 建議使用 **Chrome 或 Edge**。第一次點畫面任何地方後背景音樂才會開始播（這是瀏覽器的自動播放限制，不是 bug）。

---

## 2. 目錄結構規範

```
D:\kpop\
├── 啟動遊戲.bat                 ← 掃描題庫 + 開啟遊戲
├── 更新題庫.bat                 ← 只重建題庫
├── 產生第三關剪影.bat            ← 自動生成黑影題目影片
├── 封面\                        ← ★ 首頁照片牆用的團體照
│     IVE.jpg  NMIXX.jpg  aespa.jpg …
│     背景.jpg 或 背景.mp4          （可選）全螢幕動態背景
│     主視覺.png                    （可選）中央主視覺大圖
│
├── Git設定.bat                  ← 一次性：建立版控並連上 GitHub
├── Git存檔.bat                  ← 每次改完：存一版並推上 GitHub
├── 建立網頁版.bat                ← 打包成可部署的網頁版
├── docs\                        ← 打包產出的網頁版（拖去 Netlify 就有網址）
├── 使用說明.md                  ← 本文件
│
├── 第一關_偶像快看快答\           # 直接放偶像照片
│     IVE_WonYoung.png
│     NMIXX_Bae.jpg
│
├── 第二關_聽前奏猜歌\
│     ├── 題目\  ITZY_CAKE.mp3           # 前奏音檔（或短影片）
│     └── 答案\  ITZY_CAKE.mp4           # 完整 MV / 精華片段
│
├── 第三關_看舞蹈猜歌\
│     ├── 原始MV\ AESPA_Whiplash.mp4     # ★ 丟原版 MV 進來就好
│     ├── 題目\  AESPA_Whiplash.mp4      # 剪影靜音版（可自動生成）
│     └── 答案\  AESPA_Whiplash.mp4      # 原版 MV
│
├── 第四關_看五官猜偶像\
│     ├── 題目\ ├── 眼睛\ IVE_Gaeul.png
│     │        ├── 嘴巴\ IVE_Liz.png
│     │        └── 手燈\ IVE.png          # 檔名 = 團體名
│     └── 答案\ ├── 眼睛\ IVE_Gaeul.png   # 該偶像完整原圖
│              └── 嘴巴\ IVE_Liz.png
│
├── 介面音樂\  IVE_HEYA.mp3               # 背景音樂（可放多首，目前取第一首）
│
└── app\                                 # 程式本體，平常不用動
      ├── index.html          主程式（React + Tailwind，單檔）
      ├── styles.css          Tailwind 編譯後樣式
      ├── quiz-data.js        ★ 自動產生的題庫（遊戲讀這支）
      ├── quiz-data.json      同一份題庫的純 JSON，方便檢視
      ├── vendor\             離線版 React / ReactDOM / Babel
      └── scripts\
            ├── scan_assets.ps1      題庫掃描器
            ├── make_silhouette.ps1  第三關自動題目產生器（主控）
            ├── blackout_people.py   AI 人物去背（背景保留、人物全黑）
            ├── git_setup.ps1        版控初始化
            ├── git_save.ps1         存檔並推送
            ├── git_common.ps1       Git 共用函式（智慧推送）
            └── build_web.ps1        打包網頁版
      └── models\
            └── rvm_mobilenetv3_fp32.onnx   人物去背模型
```

### 🔑 檔名命名規則（最重要）

**所有素材一律用 `團體_名稱.副檔名`**，底線前是團體、底線後是偶像名／歌名。

```
IVE_WonYoung.png            → 團體 IVE，偶像 WonYoung
ILLIT_Cherish(My Love).mp3  → 團體 ILLIT，歌名 Cherish(My Love)
IVE.png（手燈資料夾）        → 團體 IVE
```

第二關與第三關的「題目」與「答案」檔案 **主檔名必須一模一樣**，系統靠檔名配對。

---

## 3. 新增題目流程

### 第一關 · 偶像快看快答
把照片命名成 `團體_偶像.png` 丟進 `第一關_偶像快看快答\` → 執行 `更新題庫.bat`。

- 遊戲中點「閃現照片」→ 3、2、1 倒數 → 照片只出現 **1 秒**（可在首頁調 0.3～3 秒）自動隱藏。
- 「公布答案」顯示原圖 + 團體 + 姓名。

### 第二關 · 聽前奏猜歌
`題目\` 放前奏音檔、`答案\` 放完整 MV，兩邊主檔名相同。

- 有「再聽一次」按鈕可無限重播。
- **沒放前奏檔也能玩**：系統會自動改播答案影片的前 12 秒音訊（只出聲不出畫）。想改秒數就編輯 `quiz-data.js` 裡該題的 `introSeconds`。

### 第三關 · 看舞蹈猜歌
兩種做法，二選一（見下方第 5 節）：

- **A. 什麼都不用做**：只把原版 MV 放進 `答案\`，前端會即時把畫面壓成黑影並靜音當題目。
- **B. 事先產生剪影檔**：把 MV 放進 `原始MV\`，執行 `產生第三關剪影.bat`，自動輸出 `題目\` 與 `答案\`。

只要 `題目\` 裡有同名檔案，遊戲就直接播它、不再套即時濾鏡。

### 第四關 · 看五官猜偶像
`題目\眼睛\`、`題目\嘴巴\`、`題目\手燈\` 各放局部圖，`答案\眼睛\`、`答案\嘴巴\` 放對應偶像的完整原圖（主檔名相同）。

- **預設配對規則**：眼睛與嘴巴依檔名排序後 **一對一配對**（第 1 張眼睛配第 1 張嘴巴）。
- **想自己指定配對**：在 `第四關_看五官猜偶像\` 建立 `配對.csv`，一行一題（不含副檔名）：

  ```csv
  # 眼睛檔名,嘴巴檔名,手燈檔名(可省略)
  IVE_Gaeul,ITZY_Yeji,IVE
  NMIXX_Bae,ILLIT_Wonhee,NMIXX
  ```

- 手燈沒指定時，系統會自動找「與眼睛同團體」的手燈圖。

---

## 4. 題庫 JSON 結構

`app\quiz-data.js` 是自動產生的，內容就是一個 JSON 掛在 `window.QUIZ_DATA`：

```jsonc
{
  "generatedAt": "2026-08-16T04:32:04",
  "root": "D:\\kpop",
  "bgm": ["介面音樂/IVE_HEYA.mp3"],
  "levels": {
    "level1": {
      "id": "level1", "title": "偶像快看快答",
      "questions": [
        { "id": "L1-001", "group": "IVE", "name": "WonYoung",
          "image": "第一關_偶像快看快答/IVE_WonYoung.png" }
      ]
    },
    "level2": {
      "questions": [
        { "id": "L2-001", "group": "ITZY", "title": "CAKE",
          "clip":   "第二關_聽前奏猜歌/題目/ITZY_CAKE.mp3",
          "answer": "第二關_聽前奏猜歌/答案/ITZY_CAKE.mp4",
          "introSeconds": 12 }
      ]
    },
    "level3": {
      "questions": [
        { "id": "L3-001", "group": "AESPA", "title": "Whiplash",
          "question": "第三關_看舞蹈猜歌/題目/AESPA_Whiplash.mp4",
          "answer":   "第三關_看舞蹈猜歌/答案/AESPA_Whiplash.mp4",
          "autoMask": false }
      ]
    },
    "level4": {
      "questions": [
        { "id": "L4-001",
          "eyes":  { "group": "IVE", "name": "Gaeul",
                     "q": "…題目/眼睛/IVE_Gaeul.png", "a": "…答案/眼睛/IVE_Gaeul.png" },
          "mouth": { "group": "IVE", "name": "Liz",
                     "q": "…題目/嘴巴/IVE_Liz.png",  "a": "…答案/嘴巴/IVE_Liz.png" },
          "lightstick": { "group": "IVE", "image": "…題目/手燈/IVE.png" } }
      ]
    }
  }
}
```

所有路徑都是**相對於 `D:\kpop`** 的，所以整個 `kpop` 資料夾可以隨意搬到別台電腦或隨身碟，照樣能跑。

`autoMask: true` 代表這題沒有現成剪影檔，前端會即時遮蔽。

> 想手動改題目（例如改歌名、改 `introSeconds`）可以直接編輯 `quiz-data.js`，但下次執行 `更新題庫.bat` 會被覆蓋。

---

## 5. 第三關自動化黑影 — 方案評估與使用

### 方案 A：前端即時濾鏡（預設，推薦日常使用）

**已內建，什麼都不用裝。** 只要題庫裡有答案 MV，遊戲就會：

1. 把 `<video>` 設 `muted` → 完全沒有音訊洩題
2. 依設定套用遮蔽（首頁「進階設定」可即時切換）：

| 模式 | 原理 | 適合 |
|---|---|---|
| **全黑剪影** | `grayscale(1) contrast(1000%) brightness(0.4)` 極限對比二值化 | 深色舞台、暗背景 MV |
| **模糊黑影** | 先 `blur` 吃掉細節再壓黑 | 服裝顏色太搶眼時 |
| **馬賽克** | Canvas 逐格降到 16～120px 再放大，`image-rendering: pixelated` | 想保留動作但看不出臉 |
| **暗房模式** | 極低亮度 + 高對比，只剩亮部律動 | 有燈光特效的 MV |

還有「遮蔽強度」滑桿可即時調（馬賽克模式下同時控制格子大小）。

- ✅ 優點：零處理時間、零硬碟空間、任何新 MV 丟進去馬上能出題、隨時可調
- ⚠️ 限制：是**影像濾鏡**不是 AI 去背，白色背景的 MV 可能人物壓不黑（把「遮蔽強度」拉高或改用「模糊黑影」即可）

### 方案 B：AI 人物去背（**推薦，背景保留原樣、只有人物全黑**）

```
D:\kpop\第三關_看舞蹈猜歌\原始MV\   ← 把原版 MV 丟這裡
```

然後雙擊 `產生第三關剪影.bat`（預設就是這個模式）。它會用 **RVM（Robust Video Matting）人物去背模型** 逐格把人抓出來塗黑，**練舞室、燈光、地板、場景全部保留原本的顏色**，只有人變成純黑剪影，音訊也一併抽掉。這個模型是專門為影片設計的，邊緣乾淨、不會逐格閃爍。

輸出：`題目\<同名>.mp4`（人物全黑、無音訊）與 `答案\<同名>.mp4`（原版），最後自動重建題庫。

**第一次執行會問你要不要自動下載工具：**

```
第一次執行需要先準備這些工具：
   - ffmpeg（影片轉檔，約 80 MB）
   - 免安裝版 Python + AI 套件（約 130 MB）
全部只會下載到 D:\kpop\tools，不會安裝到系統、不會改登錄檔。
要現在自動下載嗎？(Y/N)
```

按 `Y` 全自動搞定。用的是 **免安裝版（embeddable）Python**，整包關在 `D:\kpop\tools\python\` 裡，不會動到你電腦原本的環境，不想要了直接刪掉 `tools` 資料夾就好。下載完成後之後每次執行都是直接開跑。

**常用指令**（參數直接接在 bat 後面，在 `cmd` 視窗執行）：

```bat
產生第三關剪影.bat
產生第三關剪影.bat -Dilate 5                 :: 頭髮邊緣還有殘影時，把黑色範圍再外擴
產生第三關剪影.bat -Start 45 -End 75         :: 只取 45~75 秒的副歌舞蹈
產生第三關剪影.bat -MaxWidth 1280 -Fps 60    :: 要更清楚更順（處理時間會變長）
產生第三關剪影.bat -Overwrite                :: 重做已存在的輸出
產生第三關剪影.bat -Mode threshold           :: 改用不需 AI 的舊模式
```

| 參數 | 預設 | 說明 |
|---|---|---|
| `-Mode` | ai | `ai` = 背景保留人物全黑；`threshold` = 整個畫面壓成黑白剪影（不需 Python） |
| `-Hardness` | 0.7 | 剪影邊緣硬度 0~1，越大邊緣越銳利 |
| `-Dilate` | 3 | 黑色範圍外擴的像素數，調大可蓋掉髮絲殘影 |
| `-Fps` | 30 | 題目影片影格率上限，調低處理更快 |
| `-MaxWidth` | 960 | 題目影片寬度上限 |
| `-Crf` | 23 | 題目影片畫質（越小越清楚、檔案越大） |
| `-Ratio` | 自動 | 模型內部縮放 0~1，調小更快但邊緣較粗 |
| `-AnswerMaxWidth` | 1280 | 答案影片寬度上限（設 `0` = 原檔複製不轉檔） |
| `-Start` / `-End` | 無 | 只擷取指定秒數區間 |
| `-Overwrite` | 關 | 覆蓋已存在的輸出檔 |

**處理速度參考**：30 秒的 1248×576 影片，一般筆電約 1～3 分鐘。想快一點就把 `-MaxWidth` 調到 640、`-Fps` 調到 24。

> 💡 你目前的 `IVE_ATTITUDE.mp4` 有 171 MB，把它丟進 `原始MV\` 跑一次 `-AnswerMaxWidth 1280` 通常能壓到 1/5 大小，載入會明顯變順。

### 方案 C：門檻模式（`-Mode threshold`，完全不需要 Python）

把整個畫面依亮度壓成黑白二值圖 —— **背景也會一起變黑白**，跟方案 B 不一樣。只需要 ffmpeg，速度極快。適合趕時間、或 AI 模式跑不動的時候。

```bat
產生第三關剪影.bat -Mode threshold -Threshold 130   :: 人物壓更黑
產生第三關剪影.bat -Mode threshold -Invert          :: 深色背景 MV 用
```

---

## 6. 背景音樂與音訊防衝突

- 首頁與遊戲中會**循環播放** `介面音樂\` 的第一首。
- **自動 Ducking**：進入第二關播前奏、或任一關公布答案播影片時，BGM 自動 0.45 秒淡出到 5% 音量；音訊／影片結束或切到下一題時再淡回原音量。用的是計數式管理，多個音源同時觸發也不會互相蓋掉。
- 右上角常駐 **🎵 BGM 開／🔇 BGM 關** 切換鈕（遊戲中在頂部工具列）。
- 首頁「進階設定」可調 BGM 基準音量。
- 第三關題目影片一律 `muted`，不可能提前聽到歌。

---

## 7. 遊戲操作

**首頁**：勾選要挑戰的關卡 → 各關可選 5／10／15／全部／自訂抽題數（隨機抽） → 設定隊伍（1～6 隊，可改名）→ 倒數秒數、閃現時間、遮蔽方式 → 開始挑戰。

**遊戲中**：
- 頂部：關卡標籤、第幾題／共幾題、進度條、圓形倒數計時（可暫停）、BGM 開關、回首頁
- 中間：題目區 →「公布答案 🎉」→「下一題 →」
- 也有「← 上一題」與「跳過 →」隨時跳題
- 底部：各隊計分板，`＋` / `－` 即時加減分（會播答對／答錯音效）

**結束**：自動排名，金銀銅牌，可「再玩一次」（重新隨機抽題）。

---

## 8. 疑難排解

| 症狀 | 解法 |
|---|---|
| 首頁說「找不到題庫檔」 | 執行 `更新題庫.bat` |
| 執行 bat 出現「無法載入檔案…執行原則」 | bat 已加 `-ExecutionPolicy Bypass`，若仍失敗請以系統管理員開 PowerShell 執行 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` |
| 題目數量是 0 | 檢查資料夾名稱是否仍以「第一關／第二關／第三關／第四關」開頭，以及素材是否放對子資料夾 |
| 影片播不出來 | 影片請用 **H.264 編碼的 .mp4**；用 `產生第三關剪影.bat` 轉一次即可轉成相容格式 |
| 背景音樂沒聲音 | 先用滑鼠點一下畫面（瀏覽器規定要有互動才能播音），或檢查右上角是不是「🔇 BGM 關」 |
| 大檔 MV 載入很慢 | 用 `產生第三關剪影.bat --answer-maxwidth 1280 --overwrite` 壓縮答案影片 |
| 改了程式沒生效 | 瀏覽器按 `Ctrl + F5` 強制重新整理 |
| bat 出現「系統找不到指定的路徑」 | 已改用 `powershell.exe` 絕對路徑呼叫，不再依賴系統 PATH；若還是出現，代表 `D:\kpop\app\scripts\` 底下的 `.ps1` 檔不見了，重新放回即可 |
| 「Python was not found」 | 不用自己裝，`產生第三關剪影.bat` 會問你要不要自動下載免安裝版 Python 到 `D:\kpop\tools\`，按 Y 即可 |
| AI 模式失敗、說套件載不起來 | 通常是缺 Microsoft Visual C++ 執行階段，裝一次 <https://aka.ms/vs/17/release/vc_redist.x64.exe> 就好；或改用 `-Mode threshold` |
| 剪影邊緣還看得到頭髮 | 加大 `-Dilate`（例如 `-Dilate 5`）再跑一次，記得加 `-Overwrite` |

---

## 9. 想自己改程式？

`app\index.html` 是**單一檔案**，用記事本／VS Code 打開就能改。裡面 `<script type="text/babel">` 區塊就是 React 元件（JSX 由內附的 Babel 在瀏覽器端即時編譯，存檔後重新整理即可看到效果，不需要建置流程）。

主要元件：

| 元件 | 負責 |
|---|---|
| `HomeScreen` | 首頁設定面板 |
| `Level1Stage` ~ `Level4Stage` | 四關題目與答案畫面 |
| `MaskedVideo` | 第三關即時剪影／馬賽克 |
| `QuizScreen` | 進度、倒數、計分板 |
| `ResultScreen` | 賽果排名 |
| `useBgm` | 背景音樂 loop 與 ducking |
| `SFX` | Web Audio 合成音效 |

樣式若要新增 Tailwind class，`app\styles.css` 是預先編譯好的；沒被編進去的 class 不會生效，這種情況直接寫行內 `style` 最快。

---

## 10. 版本控制（GitHub）

怕改壞、怕舊版本不見，就用這個。每存一版 GitHub 都會留一份完整快照，隨時能回頭看或還原。

### 第一次設定

雙擊 **`Git設定.bat`**，它會依序做完這些事：

1. **自動裝 Git** — 找不到 Git 就下載免安裝版到 `D:\kpop\tools\git\`（不裝進系統、不改登錄檔）
2. **建立 `.gitignore`** — 自動排除所有影音素材、`tools\`、`.pptx`（見下方說明）
3. **建立第一個版本**
4. **開瀏覽器到 <https://github.com/new>**，照著畫面指示建一個**空的 private repo**：
   - Repository name 隨意，例如 `kpop-quiz-king`
   - 選 **Private**
   - 下面 README / .gitignore / license 三個勾勾**全部不要勾**
   - 按 Create repository，把網址複製貼回黑色視窗
5. **推上去** — 第一次會跳出瀏覽器要你登入 GitHub 授權，登入完就自動繼續

### 之後每次改完東西

雙擊 **`Git存檔.bat`** → 它會列出這次改了哪些檔案 → 問你「這一版做了什麼？」→ 輸入一句話（或直接 Enter 用時間當版本名）→ 自動存檔並推上 GitHub。

沒有任何變更時它會直接說「不需要存檔」，不會亂建空版本。

### 版控範圍：只有程式，沒有素材

GitHub 單檔上限 100 MB，而你的 `IVE_ATTITUDE.mp4` 就有 171 MB，所以 `.gitignore` 把這些全部排除了：

| 排除 | 原因 |
|---|---|
| 四個關卡資料夾、`介面音樂\` | 影音素材太大 |
| `tools\` | ffmpeg / Python / Git，隨時可重新下載 |
| `*.pptx`、`~$*` | 你那支 5.27 GB 的簡報與 Office 暫存檔 |

**納入版控的是這 22 個檔案**：`app\` 底下全部程式（含 AI 模型）、五支 `.bat`、`使用說明.md`、`README.md`、以及 `quiz-data.js` / `quiz-data.json`（等於也備份了你的題庫清單）。整個 repo 約 15 MB。

> ⚠️ **素材要另外備份**。Git 不管素材，請自己複製一份到外接硬碟或雲端硬碟。

### 怎麼看舊版本 / 還原

- **看歷史**：GitHub 網頁上點 **Commits**，每一版都能看到當時改了哪幾行
- **下載某個舊版**：那一版右邊 `<>` → **Download ZIP**
- **整包還原**：在 `D:\kpop` 執行 `tools\git\cmd\git.exe checkout <版本代號> -- .`
- **換電腦**：`git clone` 你的 repo，再依第 2 節的目錄規範把素材放回去，執行 `更新題庫.bat` 即可

### 疑難排解

| 症狀 | 解法 |
|---|---|
| 推送失敗、沒跳出登入視窗 | 再執行一次 `Git設定.bat`，Git 認證管理員會重新要求登入 |
| repo 網址打錯 | `tools\git\cmd\git.exe -C "D:\kpop" remote set-url origin 新網址` |
| 說 repo 不是空的 | 建 repo 時勾了 Add README，到 GitHub 刪掉重開一個空的 |
| 推不上去但怕資料不見 | 不會不見，`Git存檔.bat` 是先存本機再推送，網路好了再跑一次就會補推 |
| `! [rejected] ... (fetch first)` | 見下方「推送被拒絕」 |
| `NativeCommandError` / `fatal: branch 'origin/main' does not exist` | 舊版問題，已修掉。所有 `.ps1` 改用 `$ErrorActionPreference = "Continue"` 並自己檢查 `$LASTEXITCODE`（Windows PowerShell 5.1 在 `Stop` 之下，只要 git／ffmpeg 往 stderr 寫一個字就會中斷整支腳本，連 git 的進度訊息都算） |

### 推送被拒絕（`! [rejected] HEAD -> main (fetch first)`）

意思是 **GitHub 上有一個你本機沒有的版本**。最常見的原因：建 repo 時勾了「Add a README file」，
GitHub 就自己建了一個 commit，那條歷史跟你本機的是分開的兩條，接不起來。

**`Git存檔.bat` 現在會自己處理。** 推送失敗時它會自動 `fetch` 下來判斷遠端到底有什麼：

- **遠端只有 README / LICENSE / .gitignore 這類起始檔**（你的狀況就是這個）
  → 印出遠端有哪些檔案，問你要不要用本機這份覆蓋。按 `Y` 就用 `--force-with-lease` 蓋過去。
  你本機的**所有版本紀錄都會完整保留**，只有 GitHub 上那個空的 README 被取代。

- **遠端有真實內容**（例如你在另一台電腦推過東西）
  → 自動 `pull --rebase` 把遠端的接進來、你的新版本疊在上面，再推一次。兩邊的改動都會保留。

- **自動合併撞在一起**（同一個檔案兩邊都改）
  → 自動 `rebase --abort` 還原，本機完全不受影響，然後告訴你可以怎麼選。

想跳過詢問直接指定做法：

```bat
Git存檔.bat -Mode force     :: 用本機覆蓋 GitHub（遠端的改動會不見）
Git存檔.bat -Mode rebase    :: 把 GitHub 的接進來再推
Git存檔.bat -Yes            :: 自動判斷，不要問我
```

> 這段邏輯我用「遠端只有 README」和「遠端有別台電腦的 commit」兩種情境各實測過一次，
> 都能自動修好並保住兩邊的內容。

---

## 11. 首頁封面

### 頁面流程

```
封面（標題＋遊戲開始）  →  主選單（關卡選擇／隊伍設定／進階設定）  →  挑戰畫面  →  賽果
```

- **封面**：只有標題與「遊戲開始」。動態背景一直在跑：極光光暈飄移、Canvas 星塵粒子往上浮、斜向光帶每 9 秒掃過一次，照片牆會上下浮動並隨滑鼠做視差位移。
- **主選單**：三個分頁 —— 🎯 關卡選擇、🏆 隊伍設定、⚙️ 進階設定，底部固定顯示「本場共 N 題」與「開始挑戰」。
- **挑戰畫面**：按下「開始挑戰」後進入，**背景音樂會自動停止**，避免和題目音訊打架。回主選單或看賽果時自動恢復。遊戲中仍可用右上角按鈕手動開啟。

### 換封面照片

把照片丟進 `D:\kpop\封面\` 就好，執行「更新題庫.bat」或直接開「啟動遊戲.bat」即可生效。

| 檔名 | 效果 |
|---|---|
| 任意檔名（`IVE.jpg`、`aespa.png`…） | 飄動照片牆，建議 **6～12 張**，橫式或方形最好看 |
| `背景.jpg` / `背景.png` | 全螢幕動態背景，會緩慢推近（Ken Burns 效果） |
| `背景.mp4` / `背景.webm` | 全螢幕背景影片，自動靜音循環播放 |
| `主視覺.png` | 中央的主視覺大圖，會浮在標題上方 |

- 版面預留了中央區塊給標題與按鈕，照片會自動排在四周，**不會擋到字**。
- 手機／小螢幕只顯示 4 張，桌機顯示最多 12 張。
- `封面\` 是空的時候，系統會自動借用第一關的偶像照片，所以什麼都不放也不會開天窗。

### 第二關按鈕行為

- 一開始只有 **「▶ 播放前奏」**。
- 按下去之後這顆按鈕就消失，換成 **「🔂 再聽一次」**，可以無限重播。
- 下方固定的「公布答案 / 上一題 / 跳過」全程都在。
- 換到下一題會自動重置回「播放前奏」。

---

## 12. 發布成網頁版（傳連結給朋友玩）

整個 App 本來就是純前端（HTML + CSS + JS，沒有後端、沒有資料庫），所以可以直接丟到任何免費靜態空間。

### 步驟一：打包

雙擊 **`建立網頁版.bat`**。它會：

1. 重新掃描題庫
2. 把所有素材複製到 `D:\kpop\docs\assets\`，**檔名與資料夾全部改成英數**（中文路徑放到網路上最容易出包）
3. 用 ffmpeg 把影片壓成 854px 寬、CRF 30 的網頁版本
4. 產生 `docs\quiz-data.js`（路徑指向 `assets/`）
5. 附上 `docs\部署說明.txt`

實測你的 `ITZY_CAKE.mp4`（2560×1440、101 MB）壓完只剩幾 MB，整體**素材從 131 MB 降到 12 MB，省了 91%**。三首 MV 全部放進去大概也就 30 MB 上下，任何免費空間都吃得下。

檔案想更小就加參數：

```bat
建立網頁版.bat -VideoMaxWidth 640 -VideoCrf 32
建立網頁版.bat -NoCompress          :: 不壓縮（檔案會很大，不建議）
建立網頁版.bat -Clean               :: 先清空 docs 再重建
```

> 資料夾之所以叫 `docs`，是因為 GitHub Pages 只認這個名字。拖去 Netlify 的話名字無所謂。

### 步驟二：部署（三選一）

#### 方法 A · Netlify Drop —— 最快，不用註冊、不用打指令 ⭐推薦

1. 瀏覽器開 <https://app.netlify.com/drop>
2. 把 `D:\kpop\docs` **整個資料夾拖進網頁中央的框框**
3. 等上傳完（通常 1～3 分鐘）
4. 畫面會給你一個網址，例如 `https://sparkly-cat-123456.netlify.app`
5. 這就是可以直接貼給朋友的連結，手機電腦都能開

免費註冊 Netlify 帳號可以保留網址、改成好記的名字。要更新就重新拖一次覆蓋。

#### 方法 B · GitHub Pages —— 跟你現有的版控整合

1. 在 `D:\kpop` 執行 `Git存檔.bat`（`docs\` 會一起推上去）
2. GitHub 你的 repo → **Settings** → 左邊 **Pages**
3. Source 選 `Deploy from a branch`，Branch 選 `main`，資料夾選 `/docs`，Save
4. 等 1～2 分鐘，網址是 `https://你的帳號.github.io/repo名稱/`
5. 以後跑一次 `建立網頁版.bat` + `Git存檔.bat`，線上版就自動更新

> ⚠️ 你的 repo 目前是 **Private**，GitHub Pages 在免費方案下**只支援 Public repo**。要用這個方法就得把 repo 改成 Public，不然請用方法 A。
> 另外每次重新打包都會讓 repo 多存一份影片，久了會變大；不想要的話就用方法 A，並在 `.gitignore` 加一行 `docs/`。

#### 方法 C · Vercel —— 想接自訂網域時

1. <https://vercel.com> 用 GitHub 帳號登入 → Add New → Project → 選你的 repo
2. Framework Preset 選 `Other`，Root Directory 填 `docs`
3. Build Command 留空，Output Directory 填 `.`
4. Deploy → 拿到 `https://xxx.vercel.app`

### 手機與電腦都能玩

我用 iPhone 尺寸（390×844）和桌機（1440×900）都實測過整條流程：封面照片牆、主選單三個分頁、挑戰畫面、計分板都正常，**沒有橫向溢出、沒有 404、沒有 console error**。手機橫著拿體驗最好。

### 注意事項

- 朋友第一次點畫面之後背景音樂才會播，這是瀏覽器的自動播放限制。
- 這是**公開網址**，任何拿到連結的人都看得到裡面的照片和 MV。注意版權與隱私，不要放不能公開的東西。
- 素材更新後要重跑 `建立網頁版.bat` 再重新部署一次。

---

## 13. 封面版面與圖片顯示規則

### 單螢幕，不會出現捲軸

封面是 `height: 100vh / 100dvh` + `overflow: hidden`，進入封面時還會替 `<body>` 加上 `no-scroll`，離開自動移除。所以**封面永遠只有一個螢幕，不能往下滑，也不會有橫向捲軸**；主選單和挑戰畫面則維持正常可捲動。

標題、副標、按鈕、題數標籤全部放在同一個 Flexbox 縱向置中的容器裡，間距用 `clamp()` 綁定視窗高度自動分配。字級也是跟著高度縮放：

```css
.cover-title { font-size: clamp(26px, min(8.2vh, 9vw), 84px); }
```

視窗太矮時會自動讓步：高度 ≤ 560px 隱藏題數標籤與底部提示，≤ 440px 再隱藏副標，確保標題和「遊戲開始」永遠看得到。

實測 1440×900、1366×768、1280×620、iPhone 390×844、橫向手機 844×390 五種尺寸，**全部沒有垂直或水平捲軸，容器高度剛好等於視窗高度，標題與按鈕都在可視範圍內**。

### 圖片：沒有固定比例框，原比例完整呈現

**已完全移除 `aspect-ratio: 4/3` 之類的固定比例容器。** 現在的做法是只給最大值、寬高都交給瀏覽器：

```css
.pw-main {
  display: block; margin: 0 auto;
  max-width: 100%;
  max-height: min(22vh, 200px);
  width: auto; height: auto;
  object-fit: contain;
}
```

容器依照圖片原比例自適應縮放 —— 直式照片就是直的、橫式就是橫的，**整張完整顯示，絕不裁切，也不會有黑邊留白**。實測十張照片的算繪比例與原圖比例完全一致。

中央主視覺（`封面\主視覺.png`）同樣不鎖比例，只給 `max-width: min(90vw, 640px)` 與 `max-height: 26vh`。

### 柔邊矩形

- 圓角 `border-radius: 20px`（主視覺 24px）
- 邊緣羽化：`mask-image: radial-gradient(118% 118% at 50% 50%, #000 86%, transparent 100%)`，最外圈淡出一點點，不會像貼紙硬邊
- 柔和陰影用 `filter: drop-shadow()` 而不是 `box-shadow` —— 因為 `drop-shadow` 會沿著**遮罩後的實際形狀**投影，羽化邊和陰影才會自然接在一起，不會出現「圖片淡出了但陰影還是方框」

### Hover 放大

滑鼠移上去時 `transform: scale(1.05)` 搭配 `transition: transform .3s ease`，同時亮度微升、陰影從黑色柔和過渡成粉色光暈（`filter` 也有 `.3s` 過渡），柔邊效果一併平滑放大。

**不會撐開父容器、不會產生捲軸**：照片牆本身是 `position: fixed; inset: 0; overflow: hidden` 的圖層，放大只發生在圖層內部。實測 hover 後 `transform` 確實變成 `matrix(1.05, …)`、圖片的 `offsetWidth/offsetHeight` 完全不變、頁面依然沒有任何捲軸。


---

## 14. 分享出去朋友看不到圖片？

### 先講結論：程式裡沒有任何 `D:\` 絕對路徑

我實際檢查過你的 `D:\kpop\docs\`，裡面是這樣的：

```jsonc
// docs/quiz-data.js
"assetBase": "assets",
"cover": { "photos": [ "cover/IVE.avif", "cover/NMIXX.jpg", ... ] }
```

```
docs/assets/cover/IVE.avif        ← 檔案確實在
docs/assets/cover/NMIXX.jpg
```

程式讀圖是 `assets/cover/IVE.avif` 這種**相對路徑**，從頭到尾沒有出現過 `D:\`。所以問題不是絕對路徑，是別的東西。

### 真正的兩個嫌疑犯

**1. `.avif` / `.webp` 格式，舊手機開不了** ← 最可能

你的 12 張封面裡有 3 張 `.avif`、3 張 `.webp`。AVIF 要 **iOS 16 以上**、WebP 要 iOS 14 以上才支援。朋友如果用比較舊的 iPhone，這 6 張就是純粹的空白。

**已修好**：`建立網頁版.bat` 現在會自動把 `.avif` / `.webp` 轉成 `.jpg`（有透明背景的轉 `.png`），全平台都看得到。實測你那 12 張，6 張需要轉的全部轉檔成功。

**2. 部署的是舊版本**

`docs\` 是**打包產物**，不會自己跟著素材更新。如果你先部署、之後才把封面照片丟進 `封面\`，線上那份就還是舊的。

**正確順序永遠是**：放素材 → `建立網頁版.bat` → 重新部署。

### 圖片該放哪裡

| 你要做的 | 打包後自動變成 | 程式讀到的路徑 |
|---|---|---|
| 封面照片丟 `D:\kpop\封面\` | `docs\assets\cover\` | `assets/cover/IVE.jpg` |
| 第一關照片丟 `第一關_偶像快看快答\` | `docs\assets\level1\` | `assets/level1/IVE_WonYoung.png` |
| 背景音樂丟 `介面音樂\` | `docs\assets\bgm\` | `assets/bgm/IVE_HEYA.mp3` |

**你不需要自己搬檔案或改路徑**，`建立網頁版.bat` 會處理，順便把中文資料夾名轉成英數（`封面` → `cover`），因為中文路徑在某些主機上會出包。

### 程式碼怎麼讀圖（`app\index.html`）

```js
// 本機直接開檔 → ".."；打包成網頁版 → "assets"
const ASSET_BASE = (DATA && DATA.assetBase ? String(DATA.assetBase) : "..").replace(/\/+$/, "");

function assetUrl(p) {
  if (!p) return "";
  let s = String(p).trim();

  // 1. 外連圖床（http/https///）與 data: 內嵌圖 → 原樣使用
  if (/^(https?:)?\/\//i.test(s) || /^data:/i.test(s)) return s;

  s = s.replace(/\\/g, "/");

  // 2. 誤填本機絕對路徑（D:\kpop\... 或 file:///D:/...）→ 自動砍掉磁碟機代號改成相對路徑，
  //    並在 console 印出警告，避免分享出去後別人看不到
  const abs = s.match(/^(?:file:\/\/\/)?[A-Za-z]:\/(.*)$/i);
  if (abs) { /* …取出 封面/cover/level1… 之後的片段… */ }

  // 3. 其餘一律當專案內相對路徑
  return ASSET_BASE + "/" + s.replace(/^\/+/, "").split("/").map(encodeURIComponent).join("/");
}
```

所以就算哪天你手動編輯 `quiz-data.js` 不小心貼了 `D:\kpop\封面\IVE.jpg`，程式會自己修正成相對路徑，不會整個壞掉。

### 用線上圖床（不想把圖片打包進去時）

在 `D:\kpop\封面\` 建立 **`外連圖片.txt`**，一行貼一個網址就好，不用改任何程式碼：

```text
# 一行一個網址，# 開頭是註解
https://raw.githubusercontent.com/你的帳號/kpop-quiz-king/main/docs/assets/cover/IVE.jpg
https://i.imgur.com/AbCdEfG.jpeg
https://res.cloudinary.com/你的帳號/image/upload/w_800,q_auto,f_auto/cover/IVE.jpg
```

執行 `更新題庫.bat` 後這些網址就會加進封面照片牆，打包時**不會複製檔案**，由對方瀏覽器直接去圖床抓。

各家圖床的網址格式陷阱：

| 圖床 | ✅ 正確 | ❌ 錯誤 |
|---|---|---|
| GitHub | `https://raw.githubusercontent.com/帳號/repo/main/路徑/IVE.jpg` | `https://github.com/帳號/repo/blob/main/...`（這是網頁不是圖片） |
| Imgur | `https://i.imgur.com/AbCdEfG.jpeg` | `https://imgur.com/AbCdEfG`（同上） |
| Cloudinary | `https://res.cloudinary.com/帳號/image/upload/w_800,q_auto,f_auto/IVE.jpg` | — |

判斷方法：**把網址單獨貼到瀏覽器網址列，看到的應該是「一張圖」而不是一個網頁**。

> 缺點：圖床掛掉或圖被刪，封面就會出現「找不到圖片」佔位圖。要最穩就用本機檔案打包。

### 打包時的自動健檢

`建立網頁版.bat` 跑完會把題庫裡每一個路徑跟 `docs\` 實際檔案比對一次：

```
----------------------------------------------------------
  健檢通過：27 個檔案路徑全部正確，沒有任何本機絕對路徑。
  完整清單： D:\kpop\docs\資產清單.txt
----------------------------------------------------------
```

有問題時會直接紅字列出哪些檔案缺、哪些是絕對路徑。**看到「健檢通過」才部署，就不會再發生朋友看不到圖的事。**

### 網頁上的自我診斷

圖片萬一還是載不到，畫面會顯示一個虛線框的「找不到圖片」佔位圖（不再是空白），同時 console 會印出實際網址：

```
[KPOP] 圖片載入失敗： assets/cover/IVE.jpg
檢查 docs/assets 底下是否真的有這個檔案，以及大小寫是否完全一致。
```

**朋友回報看不到圖時，請他在該頁面按 `F12` → `Console` 分頁截圖給你**，一眼就知道是哪個檔案。

> ⚠️ 大小寫：Windows 不分大小寫，但 Netlify 和 GitHub Pages **分**。`IVE.jpg` 和 `ive.jpg` 在線上是兩個不同檔案。打包腳本會保持一致，但如果你手動改過 `docs\` 裡的檔名就要注意。

### 重新部署檢查清單

1. 素材都放好了（封面照片在 `封面\`）
2. 跑 **`建立網頁版.bat`**，確認最後印出「**健檢通過**」
3. 部署整個 **`docs` 資料夾**（不是 `app`、不是 `D:\kpop`）
   - **Netlify**：把 `docs` 資料夾拖到 <https://app.netlify.com/drop>。已經部署過的話，到該站台的 **Deploys** 分頁再拖一次覆蓋
   - **GitHub Pages**：跑 `Git存檔.bat` 推上去（`docs\` 會一起推），Pages 會自動更新
   - **Vercel**：Root Directory 要填 `docs`
4. 用**無痕視窗**打開網址確認（一般視窗會讀到舊快取，看起來像沒更新）
5. 沒問題再把連結傳給朋友

第 4 步很重要 —— 重新部署後自己用一般視窗看常常還是舊的，會誤判成沒修好。
